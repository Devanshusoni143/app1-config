apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: production
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: backend-api
  ports:
    - name: http
      port: 80
      targetPort: 80
